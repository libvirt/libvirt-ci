
- defaults:
    name: global
    branch: master
    node: libvirt
    global_env: |
      MAKE='make'
      unamestr=`uname`
      if [ "$unamestr" = 'FreeBSD' ]; then
          MAKE='gmake'
      fi
    check_env: |
    smp: 3
    spam: ymankad@redhat.com libvirt-ci@redhat.com
