---
- name: '{{ name }}-build{{ variant }}'
  shell: |
    set -e
    cd {{ name }}{{ variant }}

    {{ global_env }}
    {{ local_env }}
    perl Build.PL install_base="$VIRT_PREFIX"
    perl Build
    perl Build install
  when:
    - inventory_hostname in machines
