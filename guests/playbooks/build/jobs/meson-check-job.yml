---
- name: '{{ name }}-check'
  shell: |
    set -e
    cd {{ name }}

    {{ global_env }}
    {{ local_env }}
    cd build
    if ! ninja test
    then
        cat meson-logs/testlog.txt || true
        exit 1
    fi
  when:
    - inventory_hostname in machines
