---
- name: '{{ name }}-build{{ variant }}'
  shell: |
    set -e
    cd {{ name }}{{ variant }}

    {{ global_env }}
    {{ local_env }}
    go build -v
  when:
    - inventory_hostname in machines
