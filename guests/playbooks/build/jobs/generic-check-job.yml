---
- name: '{{ name }}-check{{ variant }}'
  shell: |
    set -e
    cd {{ name }}{{ variant }}

    {{ global_env }}
    {{ local_env }}
    {{ command }}
  when:
    - inventory_hostname in machines
