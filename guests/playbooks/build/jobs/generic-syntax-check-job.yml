---
- name: '{{ name }}-{{ branch }}-syntax-check{{ variant }}'
  shell: |
    set -e
    cd {{ name }}-{{ branch }}{{ variant }}

    {{ global_env }}
    {{ local_env }}
    {{ command }}
  when:
    - inventory_hostname in machines
