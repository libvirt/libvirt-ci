---
- name: '{{ name }}-syntax-check{{ variant }}'
  shell: |
    set -e
    cd {{ name }}{{ variant }}

    {{ global_env }}
    {{ local_env }}
    cd build
    $MAKE syntax-check
  when:
    - inventory_hostname in machines
