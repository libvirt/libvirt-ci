---
- name: '{{ name }}-check'
  shell: |
    set -e
    cd {{ name }}

    {{ global_env }}
    {{ local_env }}
    cd build
    if ! $MAKE check
    then
        find -name test-suite.log -exec cat {} \;
        exit 1
    fi
  when:
    - inventory_hostname in machines
